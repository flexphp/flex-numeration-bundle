{% trans_default_domain 'numeration' %}
{% extends 'form/layout.html.twig' %}

{% block title %}{% trans %}title.edit{% endtrans %}{% endblock %}

{% block main %}
    <div class="card">
        <div class="card-header d-flex">
            <h3 class="card-header-title">
                {% trans %}title.edit{% endtrans %}
            </h3>
            <div class="toolbar ml-auto">
                <a href="{{ path('flexphp.numeration.numerations.index') }}" class="btn btn-outline-secondary">
                    <i class="fa fa-list-ol" aria-hidden="true"></i>
                    <span class="btn-text">{% trans from 'messages' %}action.list{% endtrans %}</span>
                </a>
            </div>
        </div>

        {{ form_start(form, {'action': path('flexphp.numeration.numerations.update', {id: numeration.id}), 'method': 'PUT', 'attr': {'id': 'numeration'}}) }}
            <div class="card-body">
                {{ form_widget(form) }}
            </div>
        {{ form_end(form) }}

        <div class="card-footer">
            <div class="row">
                <div class="col">
                    {{ include('@FlexPHPNumeration/numeration/_delete_form.html.twig', {numeration: numeration}, with_context = false) }}
                </div>

                <div class="col text-right">
                    <button type="submit" form="numeration" class="btn btn-primary">
                        <i class="fa fa-save" aria-hidden="true"></i>
                        <span class="btn-text">{% trans from 'messages' %}action.update{% endtrans %}</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('bundles/flexphpnumeration/js/numerations.js') }}"></script>
{% endblock %}
